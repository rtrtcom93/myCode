#include <iostream>
using namespace std;

int main() {
    int one = 1; // 'int'는 변수를 정수(음수, 양수)라는 자료형으로 사용한다는 의미
    int two = 2; // 자료형을 정해주지 않는다면 에러가 발생함.
    int sum = one + two;

    cout << "1 + 2 = " << sum << endl; 
    
    return 0;
}